"""Common entry point for converting verified source annotations to YOLO.

Implement one adapter per source format rather than assuming all datasets use the
same coordinate convention.
"""


def main() -> None:
    print("YOLO conversion scaffold: add source-specific adapters after annotation audit.")


if __name__ == "__main__":
    main()
